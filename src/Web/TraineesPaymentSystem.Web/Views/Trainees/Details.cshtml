@using TraineesPaymentSystem.Web.Models.ViewModels.Trainees
@model TraineesPaymentSystem.Web.Models.ViewModels.Trainees.TraineeDetailsViewModel

@{
    ViewData["Title"] = "Details";
    ViewData["TraineeId"] = Model.Id;

    var grid = new HtmlGrid<TraineeTaskDetailsViewModel>(Html, Model.Tasks);
    var traineeId = Model.Id;
}

<h1>@Model.Name Details</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Username)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Username)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Age)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Age)
        </dd>
    </dl>
</div>
<div>
    @*@Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |*@
    @*@Html.ActionLink("Add Task", "Create", "Tasks") |*@
    <a asp-controller="Tasks" asp-action="Create" asp-route-traineeId="@traineeId">Add Task</a> |
    <a asp-action="Index">Back to List</a>
</div>

<br/>
<h2>Tasks</h2>

@(grid)

